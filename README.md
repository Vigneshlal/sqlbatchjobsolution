# sqlbatchjobsolution

This repository contains an SQL program that resolves stored procedure execution order based on dependency rules.

## How to Run
- Load `solution.sql` into your database.
- Run the query to get ordered steps for each UNIT_NBR.
